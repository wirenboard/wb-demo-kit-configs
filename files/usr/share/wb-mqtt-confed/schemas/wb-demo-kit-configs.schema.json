{
    "type": "object",
    "title": "Settings for wb demo kit",
    "description": "Settings for wb demo kit",
    "configPath": "/etc/wb-demo-kit-configs.conf",
    "configFile": {
        "path": "/etc/wb-demo-kit-configs.conf",
        "service": "wb-rules"
    },
    "properties": {
        "coeff_power_fan": {
	    "type": "number",
            "title": "Fan emergency power correction factor",
            "default": 0,
            "propertyOrder": 1
        }
    },
    "required": ["coeff_power_fan"],
    
    "translations": {
        "ru": {
            "Settings for wb demo kit": "Настройки для wb demo kit",
            "Fan emergency power correction factor": "Коэффециент коррекции аварийной мощности вентилятора",
        }
    }
}
